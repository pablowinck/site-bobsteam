import HomePage from 'components/HomePage';
import Layout from 'components/Layout';

const Home: React.FC = () => {
    return (
        <Layout>
            <HomePage />
        </Layout>
    );
};

export default Home;
